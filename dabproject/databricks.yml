# This is a Databricks asset bundle definition for dabproject.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dabproject
  uuid: 1654ce83-3152-4d2a-98ba-d50e2ef35b78


include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-e82d6bee-4303.cloud.databricks.com/

  prod:
    mode: production
    workspace:
      host: https://dbc-e82d6bee-4303.cloud.databricks.com/
      # We explicitly deploy to /Workspace/Users/krishnasahoo99@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/krishnasahoo99@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: krishnasahoo99@gmail.com
        level: CAN_MANAGE
